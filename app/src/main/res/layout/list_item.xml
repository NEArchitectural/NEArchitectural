<?xml version="1.0" encoding="utf-8"?>
<layout xmlns:android="http://schemas.android.com/apk/res/android">

    <data>

        <variable
            name="model"
            type="com.nearchitectural.ui.models.ListItemModel" />

    </data>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="10dp">

        <androidx.cardview.widget.CardView
            android:id="@+id/card_view"
            android:layout_width="match_parent"
            android:layout_height="match_parent"
            android:layout_gravity="center">

            <RelativeLayout
                android:onClick="openPlacePage"
                android:layout_width="wrap_content"
                android:layout_height="100dp"
                android:layout_marginStart="50dp"
                android:layout_marginLeft="50dp"
                android:layout_marginRight="50dp"
                android:clickable="true"
                android:focusable="true"
                android:paddingBottom="10dp">

                <LinearLayout
                    android:layout_width="wrap_content"
                    android:layout_height="match_parent"
                    android:layout_alignParentEnd="true"
                    android:layout_alignParentRight="true"
                    android:layout_toEndOf="@id/list_item_image"
                    android:layout_toRightOf="@id/list_item_image"
                    android:orientation="vertical">

                    <TextView
                        android:id="@+id/list_item_title"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@{model.title, default=Title}"
                        android:textSize="20sp"
                        android:textStyle="bold" />

                    <TextView
                        android:id="@+id/list_item_place_type"
                        android:layout_width="wrap_content"
                        android:layout_height="wrap_content"
                        android:text="@{model.placeType, default=PlaceType}"
                        android:textSize="12sp" />

                </LinearLayout>

                <ImageView
                    android:id="@+id/list_item_image"
                    android:layout_width="150dp"
                    android:layout_height="match_parent"
                    android:layout_alignParentBottom="true" />
                <!-- TODO set contentDescription from code on each image
                android:contentDescription="" -->


            </RelativeLayout>
        </androidx.cardview.widget.CardView>
    </LinearLayout>
</layout>